Create one clean, modern feature-overview infographic for a product slide.
16:9, flat vector, on a near-white background with a very subtle cool tint.
Deep indigo for headings, borders and icons; medium grey for the smaller lines;
warm orange used only on the single diamond in the flow strip; teal only on the
two pill shapes. Rounded rectangles, thin borders, soft shadows. A clean, plain,
highly legible sans-serif throughout. Generous spacing. No photographs, no
clip-art people, no gradients, no decorative flourishes.

TOP STRIP — five small shapes in a row across the top, joined by thin arrows,
each with one bold word inside it:
  teal pill, word: "call"
  white card, word: "listen"
  orange diamond, word: "understand"
  white card, word: "speak"
  teal pill, word: "done"

MAIN AREA — eight white cards with thin indigo borders in a 4 across by 2 down
grid, filling the rest of the frame. Each card has a small indigo line icon in
its top-left corner, a bold indigo heading, and three short grey lines beneath
it, each line beginning with a small dot.

Card 1 — icon: a soundwave
  heading: Conversation
  · streaming, not waiting
  · interrupt any time
  · detection runs locally

Card 2 — icon: a globe
  heading: Languages
  · 11 Indian languages
  · detected per call
  · custom word list

Card 3 — icon: an open book
  heading: Knowledge
  · PDF, Word, web pages
  · meaning and words
  · unanswered questions logged

Card 4 — icon: two interlocking gears
  heading: Your systems
  · your APIs mid-call
  · fields extracted
  · posted back, retried

Card 5 — icon: two human figures
  heading: Handover
  · a person on request
  · caller confirms first
  · working hours honoured

Card 6 — icon: a shield
  heading: Guardrails
  · calls at once, capped
  · queue with a message
  · backup providers, alerts

Card 7 — icon: a document with a clock
  heading: Every call kept
  · recording and transcript
  · every turn timed
  · sources shown

Card 8 — icon: a sliders control panel
  heading: Console
  · all per campaign
  · no deployment needed
  · website chat widget

TEXT — every word in the image is listed here and nothing else appears
anywhere. Set it in a plain, high-contrast sans-serif and spell each word
exactly as written:

call, listen, understand, speak, done,
Conversation, streaming not waiting, interrupt any time, detection runs locally,
Languages, 11 Indian languages, detected per call, custom word list,
Knowledge, PDF Word web pages, meaning and words, unanswered questions logged,
Your systems, your APIs mid-call, fields extracted, posted back retried,
Handover, a person on request, caller confirms first, working hours honoured,
Guardrails, calls at once capped, queue with a message, backup providers alerts,
Every call kept, recording and transcript, every turn timed, sources shown,
Console, all per campaign, no deployment needed, website chat widget

No title, no subtitle, no caption, no paragraph, no page numbers, no legend, no
logo, no watermark, no signature. Keep every line short enough to fit on one
line inside its card without wrapping.
