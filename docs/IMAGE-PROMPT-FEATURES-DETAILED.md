Create one premium, highly detailed feature-overview graphic for a modern SaaS
product page. 16:9, cinematic quality, the look of a well-funded design studio's
work — not a template, not a stock infographic.

ATMOSPHERE
A deep near-black navy background carrying a soft mesh gradient: indigo bleeding
into violet at the upper left, a faint teal wash at the lower right, everything
else falling away into darkness with a gentle vignette at the edges. A very fine
dot-grid texture across the whole background, barely visible. A soft light source
from the upper left, so cards nearer that corner catch a subtle rim of light
along their top edge. Faint volumetric glow behind the brightest elements.

THE CARDS
Frosted glass panels — translucent dark surfaces with a 1px light border that
fades from bright at the top-left to nearly invisible at the bottom-right, a
subtle inner glow, and a soft deep shadow beneath. Each card is tilted a degree
or two in three dimensions so the grid has real depth rather than sitting flat.
The two cards nearest the viewer are crisp; the ones toward the edges are very
slightly out of focus.

TOP STRIP — five small glass shapes in a row across the upper area, connected by
thin glowing indigo lines with a small bright node where each line meets a shape.
Each carries one bold white word:
  a teal-glowing pill, word: "call"
  a glass card, word: "listen"
  a coral-glowing diamond, word: "understand"
  a glass card, word: "speak"
  a teal-glowing pill, word: "done"

MAIN AREA — eight glass cards in a 4 across by 2 down grid below the strip, with
generous space between them. Each card holds, in this order: a circular badge in
the top-left containing a duotone line icon drawn in indigo with a coral
highlight and a soft glow around it; a bold white heading; and three short lines
in cool light grey beneath, each preceded by a small glowing indigo dot.

Card 1 — icon: a soundwave
  heading: Conversation
  · streaming, not waiting
  · interrupt any time
  · detection runs locally

Card 2 — icon: a globe
  heading: Languages
  · 11 Indian languages
  · detected per call
  · custom word list

Card 3 — icon: an open book
  heading: Knowledge
  · PDF, Word, web pages
  · meaning and words
  · gaps logged

Card 4 — icon: two interlocking gears
  heading: Your systems
  · your APIs mid-call
  · fields extracted
  · posted back, retried

Card 5 — icon: two human figures
  heading: Handover
  · a person on request
  · caller confirms first
  · working hours kept

Card 6 — icon: a shield
  heading: Guardrails
  · calls at once, capped
  · queue with a message
  · backup providers

Card 7 — icon: a document with a clock
  heading: Every call kept
  · recording and transcript
  · every turn timed
  · sources shown

Card 8 — icon: a sliders control panel
  heading: Console
  · all per campaign
  · no deployment needed
  · website chat widget

DETAIL THAT MAKES IT FEEL REAL
Thin hairline connectors trailing faintly between neighbouring cards, like a
circuit fading into the dark. A few small light particles drifting in the
background. A soft reflection under the top strip, as though the shapes sit on
polished glass. Crisp, confident, expensive — deliberate rather than decorated.

TYPE
A clean geometric sans-serif. Headings bold and bright white with high contrast
against the dark glass. The three lines beneath in a lighter weight and a cool
grey, clearly smaller than the heading but still sharp and fully legible. Never
let type sit over a bright part of the background.

TEXT — every word in the image is listed here and nothing else appears anywhere.
Spell each exactly as written:

call, listen, understand, speak, done,
Conversation, streaming not waiting, interrupt any time, detection runs locally,
Languages, 11 Indian languages, detected per call, custom word list,
Knowledge, PDF Word web pages, meaning and words, gaps logged,
Your systems, your APIs mid-call, fields extracted, posted back retried,
Handover, a person on request, caller confirms first, working hours kept,
Guardrails, calls at once capped, queue with a message, backup providers,
Every call kept, recording and transcript, every turn timed, sources shown,
Console, all per campaign, no deployment needed, website chat widget

No title, no subtitle, no caption, no paragraph, no numbers other than the "11",
no legend, no logo, no watermark, no signature. Every line sits on one line
inside its card without wrapping.
