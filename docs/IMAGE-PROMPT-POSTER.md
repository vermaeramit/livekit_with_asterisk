Create a dense, professional technical infographic poster in a dark neon style,
1280 x 880, the kind a senior engineer posts to show off a system they built.
Sharp, information-rich, confident. Not a marketing slide — an engineering
poster.

OVERALL LOOK
Very dark navy-black background, almost #0a0e27. The whole poster is a bento
layout of rounded rectangular panels of DIFFERENT sizes, tightly packed with
small consistent gaps, filling the frame edge to edge with a small outer margin.
Every panel has a dark translucent fill and a thin 1px glowing border, and each
panel's border is a DIFFERENT neon colour — cyan, violet, green, orange, pink,
yellow, blue. Each panel has an ALL-CAPS heading in its own border colour, with
a small matching icon to the left of the heading. Body text is small, light
grey, clean sans-serif, tightly set. Small glowing line icons throughout.
Bright, saturated neon accents against near-black. High contrast, crisp edges,
no blur, no photographic elements.

TOP LEFT — the title block, no panel border around it:
  Line 1, large bold white:            Built an AI Voice Agent
  Line 2, larger bold, cyan fading to violet:   FOR A LIVE DIALLER
  Line 3, small white:                 Your documents. Your systems. Real calls.
  Line 4, two small grey lines:
      A self-hosted agent that answers the phone in Hindi,
      answers from your own documents, and hands over to a person.

TOP RIGHT — panel, cyan border, heading "TECH STACK".
A single row of seven small icons, each with a bold white word beneath it and a
tiny grey word beneath that:
  LiveKit / Media      Asterisk / Telephony     Sarvam / Speech
  OpenAI / Reasoning   pgvector / Retrieval     FastAPI / Backend
  React / Console

MIDDLE LEFT — panel, violet border, heading "WHAT CHANGES".
Two small boxes side by side inside it. Left box has a red border and a red
heading "Old IVR", with a downward chain of short lines separated by small down
arrows:
      Caller speaks
      Press 1, press 2
      Wrong menu
      Caller hangs up
Right box has a green border and a green heading "AI Agent", same downward chain:
      Caller speaks
      Understood, not matched
      Answered from your documents
      Or handed to a person

MIDDLE — panel, pink border, heading "KEY FEATURES".
A vertical list, each line preceded by a small pink circled checkmark:
      Interrupt the bot mid-sentence
      11 Indian languages
      Answers from PDF, Word and web pages
      Calls your own APIs mid-call
      Transfers to a human on request
      Recording, transcript and timings
      Concurrency limit with a hold queue
      Everything set per campaign

RIGHT, LARGE — the biggest panel, blue border, heading "HOW IT WORKS".
It holds two labelled pipelines stacked vertically, separated by a faint
dashed line.

  A small numbered badge "1" then the sub-heading "A CALL, END TO END", and
  beneath it a horizontal pipeline of six icons joined by right-facing arrows,
  each icon with two small lines of text under it:
      Dialler / connects        Asterisk / limit checked
      LiveKit / media           Agent / listens
      Providers / speech, model, voice
      Caller / hears the answer

  A small numbered badge "2" then the sub-heading "WHERE THE ANSWER COMES FROM",
  and beneath it a second horizontal pipeline of five icons with arrows:
      Question / in Hindi       Search / in English
      pgvector / meaning + words
      Top passages / from your documents
      Answer / grounded, not guessed

  Beneath the two pipelines, a small mock chat window with a rounded border, a
  tiny window title "Call 538", one dark bubble containing
  "Does this model have an idle start-stop system?" and one lighter bubble
  containing "It does not. Here is what it has instead." and beneath that a row
  of two tiny tags reading "Brochure p4" and "Spec sheet".

LOWER LEFT — panel, yellow border, heading "WHAT YOU CONFIGURE".
A two-column list, a small file-like icon at the left of each row, the name in
white monospace and a short grey description beside it:
      Prompt        instructions, greeting, markers
      Voice         provider, model, speed, preview
      Knowledge     documents, thresholds, filler line
      Tools         your APIs, auth, parameters
      Limits        turns, duration, calls at once
      Handover      dialler, extension, working hours

LOWER MIDDLE — panel, orange border, heading "WHAT WAS TUNED".
A list, each line preceded by a small orange checkmark, with the numbers
prominent:
      Prompt build          1154 ms  to  339 ms
      Ring before answer    2.07 s  to  0.75 s
      First spoken word     4.03 s  to  2.4 s
      Greeting              synthesised once, then cached
      Prompt prefix         byte-identical, so it caches

BOTTOM LEFT — panel, green border, heading "MEASURED, NOT CLAIMED".
Two small boxes side by side. Left box, red border, red heading "Before":
      4.03 s to the first word
      every greeting re-synthesised
      one provider, no fallback
Right box, green border, green heading "After":
      2.4 s to the first word
      greeting served from disk
      second provider on standby
      20 calls at once, clean
To the right of the two boxes, a small semicircular speedometer gauge glowing
teal, with "2.4s" in large text beneath it and the small grey words
"to the first word".

BOTTOM RIGHT — panel, violet border, heading "NOT DONE YET".
A row of four small icons, each with two short lines of text beneath:
      Voice on the web widget / text only today
      Scanned pages / no OCR yet
      Real queue order / first come, first served
      Speech to speech / worth benchmarking

TYPE AND LEGIBILITY
A clean geometric sans-serif everywhere. Headings ALL-CAPS, bold, letter-spaced,
in each panel's own neon colour. Body text small but perfectly sharp and fully
readable, light grey on dark. Numbers bold and bright. Nothing overlaps, nothing
is cut off, every line sits inside its panel.

Spell every word exactly as written above. Do not add a logo, a watermark, a
signature, a URL, or any text that is not listed here.
