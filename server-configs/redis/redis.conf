# Deployed at /opt/aivoice/redis/redis.conf

bind 127.0.0.1
port 6379
protected-mode yes

# Persistence OFF. This is LiveKit coordination state, not durable data -
# and disk I/O on this box adds jitter to real-time audio.
save ""
appendonly no

maxmemory 256mb
maxmemory-policy allkeys-lru
