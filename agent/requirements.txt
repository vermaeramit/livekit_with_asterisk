asyncpg==0.31.0
av==18.0.0
# Fernet, for the per-client provider keys in crypto.py
cryptography==46.0.3
indic_transliteration==2.3.82
livekit==1.1.13
livekit-agents==1.6.7
livekit-api==1.2.0
livekit-blingfire==1.1.0
livekit-local-inference==0.2.6
# Gemini is the LLM fallback. It was installed by hand during the Step 10b
# benchmarks and never declared - a rebuilt venv would have dropped it and the
# fallback chain would have failed to import at worker start.
livekit-plugins-google==1.6.7
livekit-plugins-openai==1.6.7
livekit-plugins-sarvam==1.6.7
livekit-plugins-silero==1.6.7
# Selectable per campaign alongside sarvam and openai. Pinned to 1.6.7 to
# match everything else - the plugin's own latest (1.6.9) requires
# livekit-agents>=1.6.9, which would drag the whole stack forward.
livekit-plugins-soniox==1.6.7
livekit-plugins-turn-detector==1.6.7
livekit-protocol==1.1.21
numpy==2.5.1
onnxruntime==1.28.0
openai==2.51.0
pymupdf==1.28.0
pymupdf-layout==1.28.0
pymupdf4llm==1.28.0
tiktoken==0.13.0
# Word documents for the knowledge base. Chosen over mammoth/pandoc: one
# dependency, no external binary, and direct access to paragraph STYLES -
# which is what lets a "Heading 2" become "## " for the chunker to split on.
python-docx==1.1.2
